# Ответы по теме `1С:Элемент.Скрипт`

## 1. Какие циклы существуют в `1С:Элемент.Скрипт` и чем они отличаются?

В языке используются 3 основных цикла:

### `для по`
Применяется, когда нужен цикл со счетчиком: известны начало, конец и, при необходимости, шаг.

```sbsl
для Индекс = 1 по 10
    Консоль.Записать(Индекс)
;
```

### `для из`
Применяется для обхода коллекций и строк по элементам.

```sbsl
для Символ из "abc123"
    Консоль.Записать(Символ)
;
```

### `пока`
Используется, когда количество повторений заранее неизвестно. Цикл выполняется, пока условие истинно.

```sbsl
пока Число > 0
    Число -= 1
;
```

### Отличия
- `для по` — цикл по диапазону значений.
- `для из` — цикл по элементам коллекции.
- `пока` — цикл по условию.

---

## 2. Какие есть способы разбиения сложной строки на составные части?

Основные способы:

### `Разделить()`
Разбивает строку по разделителю или регулярному выражению.

```sbsl
знч Части = "Иван,Петр,Анна".Разделить(",")
```

```sbsl
знч Части = "a1б2C3".Разделить('[\ц]')
```

### `ПолучитьСтроки()`
Используется для многострочного текста.

```sbsl
знч СтрокиТекста = Текст.ПолучитьСтроки()
```

### `Найти()`, `НайтиСКонца()`, `Подстрока()`
Подходят для ручного разбиения строки по индексам.

```sbsl
знч Позиция = Строка.Найти("-")
знч ЛеваяЧасть = Строка.Подстрока(0, Позиция)
```

### Посимвольный разбор
Используются `для из`, `Символ()`, `Символы.ЭтоБуква()`, `Символы.ЭтоЦифра()`.

---

## 3. Какие есть условные конструкции в `1С:Элемент.Скрипт`? Как они используются в коде?

В языке есть:

- `если`
- `выбор`
- тернарный оператор `? :`

### `если`

```sbsl
если Возраст >= 18
    Консоль.Записать("Доступ разрешен")
иначе
    Консоль.Записать("Доступ запрещен")
;
```

### `выбор`

```sbsl
выбор Операция
когда "Сумма"
    Консоль.Записать("Считаем сумму")
когда "Среднее"
    Консоль.Записать("Считаем среднее")
иначе
    Консоль.Записать("Неизвестная операция")
;
```

### Тернарный оператор

```sbsl
знч Результат = Возраст >= 18 ? "Взрослый" : "Несовершеннолетний"
```

---

## 4. Каким методом можно удалить знаки препинания? Является ли этот метод универсальным для всех символов?

Чаще всего используют метод `Заменить()`.

```sbsl
знч БезПунктуации = Текст.Заменить('[.,!?:;()-]', "")
```

Если нужно удалить конкретную подстроку, можно использовать `Удалить()`:

```sbsl
знч БезТочек = Текст.Удалить(".")
```

### Вывод
- `Удалить()` не универсален: удаляет только указанную строку.
- Для удаления набора символов удобнее `Заменить()` с регулярным выражением.
- Полностью универсального встроенного метода для удаления всей пунктуации Юникода нет.

---

## 5. Какие методы в `1С:Элемент.Скрипт` используются для работы с число-буквенными строками для извлечения числовой/буквенной части или проверки формата?

Полезные методы:

- `ТолькоЦифры()`
- `ТолькоБуквы()`
- `ТолькоБуквыИлиЦифры()`
- `Содержит(Образец)`
- `ПолноеСовпадение(Образец)`
- `Разделить(Образец)`
- `Заменить(Образец, Замена)`

Пример проверки формата:

```sbsl
если Код.ПолноеСовпадение('[A-Za-zА-Яа-я]+\ц+')
    Консоль.Записать("Формат корректен")
;
```

Пример ручного извлечения:

```sbsl
пер ТолькоЦифры = ""
пер ТолькоБуквы = ""

для Символ из ИсходнаяСтрока
    если Символ.ТолькоЦифры()
        ТолькоЦифры += Символ
    иначе если Символ.ТолькоБуквы()
        ТолькоБуквы += Символ
    ;
;
```

---

## 6. Какие объекты можно использовать в качестве «коллекции» для цикла `для из`?

В `для из` можно использовать объекты, поддерживающие обход:

- `Строка`
- `Массив`
- `Множество`
- `Соответствие`
- другие типы, реализующие `Обходимое`

### Пример со строкой

```sbsl
для Символ из "Привет"
    Консоль.Записать(Символ)
;
```

### Пример с соответствием

```sbsl
для Пара из Сотрудники
    Консоль.Записать("%{Пара.Ключ}: %{Пара.Значение}")
;
```

Для `Соответствие` элемент цикла имеет тип `КлючИЗначение`.

---

## 7. Что делают операторы `вниз`, `шаг`, `прервать` и `продолжить` в циклах?

### `вниз`
Задает обратное направление перебора.

### `шаг`
Определяет, с каким шагом меняется счетчик.

### `прервать`
Полностью завершает цикл.

### `продолжить`
Пропускает оставшуюся часть текущей итерации и переходит к следующей.

### Пример

```sbsl
для Индекс = 10 вниз по 1 шаг 2
    если Индекс == 6
        продолжить
    ;
    если Индекс == 2
        прервать
    ;
    Консоль.Записать(Индекс)
;
```

---

## 8. Для разделения число-буквенной строки на составляющие можно использовать посимвольный анализ. Какие функции для проверки типа символа существуют?

В `Стд::Символы` есть методы:

- `Символы.ЭтоБуква()`
- `Символы.ЭтоЦифра()`
- `Символы.ЭтоБукваИлиЦифра()`
- `Символы.ЭтоПробельный()`
- `Символы.ЭтоВерхнийРегистр()`
- `Символы.ЭтоНижнийРегистр()`

### Пример

```sbsl
для Символ из Строка
    если Символы.ЭтоЦифра(Символ)
        ТолькоЦифры += Символ
    иначе если Символы.ЭтоБуква(Символ)
        ТолькоБуквы += Символ
    ;
;
```

---

## 9. Какой синтаксис у тернарного оператора и в чем его главное отличие от инструкции `если`?

### Синтаксис

```sbsl
логическое_выражение ? выражение_если_истина : выражение_если_ложь
```

### Пример

```sbsl
знч Статус = Балл >= 60 ? "Сдано" : "Не сдано"
```

### Главное отличие
- `если` — это инструкция.
- `? :` — это выражение, которое возвращает значение.

Поэтому тернарный оператор удобно использовать в присваиваниях и `возврат`.

---

## 10. Инструкция `выбор`: синтаксис. Отличия полной и предикатной формы

## Синтаксис

```sbsl
выбор [выражение_выбора]
когда выражение_сравнения[, выражение_сравнения]
    инструкции
[когда ...]
[иначе
    инструкции]
;
```

## Полная форма

Используется, когда одно значение сравнивается с несколькими вариантами.

```sbsl
выбор Команда
когда "start"
    Консоль.Записать("Запуск")
когда "stop"
    Консоль.Записать("Остановка")
иначе
    Консоль.Записать("Неизвестная команда")
;
```

Здесь фактически выполняются проверки:

```sbsl
Команда == "start"
Команда == "stop"
```

## Предикатная форма

Используется, когда в ветках указываются полноценные логические условия.

```sbsl
выбор
когда Балл >= 90
    Консоль.Записать("Отлично")
когда Балл >= 75
    Консоль.Записать("Хорошо")
иначе
    Консоль.Записать("Нужно подтянуть")
;
```

## Отличия
- Полная форма: есть выражение выбора, удобно сравнивать одно значение с набором вариантов.
- Предикатная форма: выражения выбора нет, каждая ветка содержит отдельное логическое условие.

---

## Источники

- [Цикл `для по`](https://1cmycloud.com/console/help/lang/docs/topics/for-loop/)
- [Цикл `для из`](https://1cmycloud.com/console/help/lang/docs/topics/for-in-loop/)
- [Цикл `пока`](https://1cmycloud.com/console/help/lang/docs/topics/while-loop/)
- [Инструкция `если`](https://1cmycloud.com/console/help/lang/docs/topics/if-statement/)
- [Инструкция `выбор`](https://1cmycloud.com/console/help/lang/docs/topics/case-statement/)
- [Тернарная операция `?`](https://1cmycloud.com/console/help/lang/docs/topics/question-mark-operation/)
- [Тип `Строка`](https://1cmycloud.com/console/help/lang/docs/stdlib/Std/String_ru/)
- [Тип `Символы`](https://1cmycloud.com/console/help/lang/docs/stdlib/Std/Chars_ru/)
- [Регулярные выражения](https://1cmycloud.com/console/help/lang/docs/stdlib/Std/RegularExpressions/)
